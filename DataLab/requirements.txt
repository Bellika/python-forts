# Core dependencies for DataLab
# This file contains only the packages needed to run DataLab

# Currently DataLab uses only Python standard library
# No external dependencies required for basic functionality

# Optional: Data science libraries (uncomment when needed)
# pandas>=2.0.0
# numpy>=1.24.0
# matplotlib>=3.7.0
# seaborn>=0.13.0

# Development dependencies (testing, linting, formatting)
# For CI/CD, see requirements-ci.txt
